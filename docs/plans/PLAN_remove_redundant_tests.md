# Implementation Plan: 과도하거나 의미없는 테스트 제거

> 작성/운영 규칙(SoT): 반드시 [docs/CLAUDE.md](../CLAUDE.md)를 참고하세요.
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 [docs/CLAUDE.md](../CLAUDE.md)를 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: [docs/CLAUDE.md](../CLAUDE.md) 섹션 3, 5 참고

---

**작성일**: 2026-01-16 12:00
**마지막 업데이트**: 2026-01-16 12:15
**관련 범위**: tests
**관련 문서**: tests/CLAUDE.md

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 [docs/CLAUDE.md](../CLAUDE.md)를 따릅니다.

- Validation에서 `poetry run python validate_project.py`가 실패하면 **해당 Phase에서 즉시 수정 후 재검증**한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 과도하거나 의미없는 테스트 4개 제거
- [x] 테스트 코드베이스 품질 향상 (중복 및 의미없는 테스트 정리)

## 2) 비목표(Non-Goals)

- 핵심 비즈니스 로직 테스트 변경
- 새로운 테스트 추가
- 테스트 로직 리팩토링

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

테스트 코드 분석 결과 다음과 같은 문제가 발견되었습니다:

1. **test_numpy_warnings.py의 `test_without_fixture_no_warning_by_default`**
   - 마지막 assert가 `warning_count >= 0`으로 **항상 참**
   - 실제로 아무것도 검증하지 않는 테스트

2. **test_strategy.py의 `test_grid_search_result_sorting`**
   - `test_basic_grid_search`에서 이미 정렬 검증 수행
   - 중복 테스트

3. **test_formatting.py의 TestAlign 클래스 (2개 테스트)**
   - `test_enum_values`: Enum 값 확인 (매우 단순)
   - `test_enum_comparison`: Enum 비교 확인 (매우 단순)
   - Enum 값은 사용처에서 자연스럽게 검증됨

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `CLAUDE.md` (루트)
- `tests/CLAUDE.md`

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 docs/CLAUDE.md)

- [x] 기능 요구사항 충족 (4개 테스트 제거)
- [x] `poetry run python validate_project.py` 통과 (failed=0, skipped=0)
- [x] `poetry run black .` 실행 완료 (마지막 Phase에서 자동 포맷 적용)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일

- `tests/test_numpy_warnings.py` - `test_without_fixture_no_warning_by_default` 제거
- `tests/test_strategy.py` - `test_grid_search_result_sorting` 제거
- `tests/test_formatting.py` - `TestAlign` 클래스 전체 제거 (test_enum_values, test_enum_comparison)

### 데이터/결과 영향

- 없음 (테스트만 제거)

## 6) 단계별 계획(Phases)

### Phase 1 — 테스트 제거 (그린 유지)

**작업 내용**:

- [x] `tests/test_numpy_warnings.py`에서 `test_without_fixture_no_warning_by_default` 메서드 제거
- [x] `tests/test_strategy.py`에서 `test_grid_search_result_sorting` 메서드 제거
- [x] `tests/test_formatting.py`에서 `TestAlign` 클래스 전체 제거

**Validation**:

- [x] `poetry run python validate_project.py` (passed=205, failed=0, skipped=0)

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

- [x] `poetry run black .` 실행(자동 포맷 적용)
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정

**Validation**:

- [x] `poetry run python validate_project.py` (passed=205, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

1. 테스트 / 과도하거나 의미없는 테스트 4개 제거
2. 테스트 / 중복 및 무의미한 테스트 정리
3. 테스트 / 테스트 코드 품질 개선 (불필요 테스트 제거)
4. 테스트 / 항상 참인 테스트 및 중복 테스트 제거
5. 테스트 / 테스트 정리 (test_numpy_warnings, test_strategy, test_formatting)

## 7) 리스크(Risks)

- 낮음: 테스트만 제거하므로 프로덕션 코드에 영향 없음
- 제거하는 테스트가 다른 테스트와 의존성이 없는지 확인 필요

## 8) 메모(Notes)

- 제거 대상 테스트는 사용자 확인 후 결정됨

### 진행 로그 (KST)

- 2026-01-16 12:00: 계획서 작성
- 2026-01-16 12:10: 테스트 4개 제거 완료
- 2026-01-16 12:15: 검증 통과 및 계획서 완료
