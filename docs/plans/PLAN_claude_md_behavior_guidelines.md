# Implementation Plan: 루트 CLAUDE.md 행동 지침 보강

> 작성/운영 규칙(SoT): 반드시 [docs/CLAUDE.md](../CLAUDE.md)를 참고하세요.
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 [docs/CLAUDE.md](../CLAUDE.md)를 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: [docs/CLAUDE.md](../CLAUDE.md) 섹션 3, 5 참고

---

**작성일**: 2026-05-03 10:17
**마지막 업데이트**: 2026-05-03 10:25
**관련 범위**: 프로젝트 전반 (루트 CLAUDE.md)
**관련 문서**: [CLAUDE.md](../../CLAUDE.md), [CLAUDE_andrej-karpathy.md](../../CLAUDE_andrej-karpathy.md), [기본 프롬프트.md](../../기본%20프롬프트.md)

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 [docs/CLAUDE.md](../CLAUDE.md)를 따릅니다.

- `poetry run python validate_project.py`는 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: `CLAUDE_andrej-karpathy.md`(하네스 엔지니어링 가이드)의 핵심 행동 지침을 루트 `CLAUDE.md`에 한국어로 통합한다.
- [x] 목표 2: `기본 프롬프트.md`(세션 시작 시 매번 입력하는 프롬프트)에서 반복 적용되는 절차/원칙을 루트 `CLAUDE.md`에 통합하여 매 세션 입력 부담을 줄인다.
- [x] 목표 3: 행동 변화에 직결되는 신규 섹션(사고 절차, 수술적 변경, 목표 주도 실행)을 문서 **상단**에 배치하여 AI 모델 캐치율을 높인다.

## 2) 비목표(Non-Goals)

- 코드 변경 없음 (마크다운 문서 수정만 수행)
- 기존 CLAUDE.md 섹션의 의미/규칙 변경 없음 (보강만 수행)
- 도메인별 CLAUDE.md(예: `src/qbt/CLAUDE.md`, `src/live/CLAUDE.md`) 수정 없음
- 두 참고 파일(`CLAUDE_andrej-karpathy.md`, `기본 프롬프트.md`)의 삭제/이동은 본 plan 범위 밖 (사용자 결정에 위임 — Notes 참고)

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- 현재 루트 `CLAUDE.md`에는 프로젝트 구조/코딩 표준은 잘 정리되어 있으나, **AI 모델의 작업 수행 방식**(가정 명시, 추측 금지, 인접 코드 임의 수정 금지 등)에 대한 행동 지침이 부족하다.
- 사용자는 매 세션마다 `기본 프롬프트.md`의 내용을 수동으로 입력해 왔으며, 이 부담을 CLAUDE.md 통합으로 제거할 필요가 있다.
- `CLAUDE_andrej-karpathy.md`는 LLM 코딩 실수를 줄이기 위한 검증된 행동 지침이며, 본 프로젝트에 적용 시 코드 변경 품질이 향상될 것으로 기대된다.

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- 루트 [CLAUDE.md](../../CLAUDE.md) — 본 plan의 수정 대상
- [docs/CLAUDE.md](../CLAUDE.md) — 계획서 작성 규칙
- 작업 도메인 CLAUDE.md는 **수정 대상이 아니므로 영향 없음**

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 docs/CLAUDE.md)

- [x] 루트 `CLAUDE.md`에 신규 섹션 4개 추가 완료 (사고 절차 / 수술적 변경 / 목표 주도 실행 / 외부 라이브러리 학습)
- [x] 루트 `CLAUDE.md`의 기존 "개발 원칙" 섹션에 자문 체크 항목 5번 추가 완료
- [x] 신규 섹션 3개(사고 절차, 수술적 변경, 목표 주도 실행)가 "문서 목적" 섹션 직후에 배치됨
- [x] 기존 CLAUDE.md 섹션의 의미/규칙 변경 없음 (보강만 수행)
- [x] `poetry run python validate_project.py` 통과 (passed=1025, failed=0, skipped=0)
- [x] `poetry run black .` 실행 완료 (146 files left unchanged — 마크다운만 수정했으므로 변경 없음)
- [x] 필요한 문서 업데이트 — README.md 변경 없음 / `docs/COMMANDS.md` 변경 없음 / 도메인 CLAUDE.md 변경 없음
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- `CLAUDE.md` (루트) — 신규 섹션 4개 추가 + 기존 "개발 원칙" 섹션 1줄 보강
- `README.md`: 변경 없음
- `docs/COMMANDS.md`: 변경 없음 (실행 명령어/CLI 옵션 변경 없음)
- 도메인 `CLAUDE.md`: 변경 없음

### 데이터/결과 영향

- 코드 변경 없음 → 출력 스키마/결과 영향 없음
- 테스트 영향 없음
- AI 모델의 행동 변화는 발생 (의도된 변화)

## 6) 단계별 계획(Phases)

### Phase 1 — 루트 CLAUDE.md 신규 섹션 추가 및 보강

**작업 내용**:

- [x] "문서 목적" 섹션 직후, "도메인별 CLAUDE.md 참고 규칙" 직전에 신규 섹션 3개를 순서대로 추가:
  - [x] `## 사고 절차 (Think Before Coding)` — 가정 명시 / 추측 금지 / 모호 시 질문 / 질문 형식 표준화 / 여러 해석 시 모두 제시 / 단순 대안 푸시백
  - [x] `## 수술적 변경 (Surgical Changes)` — 요청 외 인접 코드 수정 금지 / 깨지지 않은 코드 리팩토링 금지 / 기존 스타일 매칭 / 본인 변경으로 생긴 orphan만 정리 / 무관한 데드코드는 언급만 / 모든 변경 라인은 사용자 요청과 직접 연결
  - [x] `## 목표 주도 실행 (Goal-Driven Execution)` — 검증 가능한 성공 기준 정의 / 단계별 verify 형식 / 계획서 섹션과의 보완 관계 명시
- [x] "계획서(Plan) 작성이 필요한 경우" 섹션 직후, "프로젝트 개요" 섹션 직전에 신규 섹션 1개 추가:
  - [x] `## 외부 라이브러리/도구 학습` — Context7 우선 / 부족 시 웹검색 보완
- [x] 기존 "개발 원칙" 섹션에 5번 항목 추가:
  - [x] `5. 자문 체크: "시니어 엔지니어가 과복잡하다고 볼까?" — 그렇다면 단순화`

**작성 원칙 준수**:

- [x] 한국어 작성 (사용자 출력 스타일 준수)
- [x] 기존 CLAUDE.md의 톤/포맷(헤더 깊이, 불릿 스타일, `>` 인용블록 강조 패턴) 일관 유지
- [x] 강조 마커(`CRITICAL`, `IMPORTANT`)는 행동 지침 핵심 규칙에만 절제하여 사용
- [x] 기본 프롬프트.md의 템플릿 골격(`[전제 조건]`, `[수행할 내용의 프롬프트]`)은 옮기지 않음 (사용자 입력용이므로)

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

- [x] 필요한 문서 업데이트 (README.md 변경 없음 / `docs/COMMANDS.md` 변경 없음 / 도메인 CLAUDE.md 변경 없음 — 모두 명시적으로 확인)
- [x] `poetry run black .` 실행(자동 포맷 적용) — 146 files left unchanged
- [x] 변경된 CLAUDE.md를 다시 읽고 흐름/일관성 최종 점검
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정

**Validation**:

- [x] `poetry run python validate_project.py` (passed=1025, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

1. 문서 / 루트 CLAUDE.md에 AI 행동 지침 4개 섹션 추가
2. 문서 / 사고 절차·수술적 변경·목표 주도 실행 지침 통합
3. 문서 / 하네스 엔지니어링 가이드 + 기본 프롬프트 규칙 통합
4. 문서 / CLAUDE.md 행동 지침 보강 — 캐치율 위해 상단 배치
5. 문서 / 작업 수행 원칙(추측 금지·인접 코드 보존·검증 기준) 추가

## 7) 리스크(Risks)

- **리스크 1**: 신규 섹션이 기존 섹션과 의미적으로 중복될 가능성
  - **완화책**: 사전 분석에서 중복 항목(YAGNI/간결성, 계획서 작성 규칙 등)은 보류 처리. "개발 원칙" 5번은 기존 1~4번과 자연스럽게 연결되도록 작성.
- **리스크 2**: CLAUDE.md가 길어지면서 후반 섹션 캐치율 저하
  - **완화책**: 행동 변화 직결 섹션은 상단 배치. 기존 섹션 순서는 그대로 유지하여 정보 구조 안정성 확보.
- **리스크 3**: 새 행동 지침과 기존 프로젝트 워크플로(예: 계획서 작성 규칙)가 충돌
  - **완화책**: "목표 주도 실행" 섹션에 "인라인 plan은 즉석 실행용, docs/plans는 공식 계획서"라는 보완 관계를 명시.

## 8) 메모(Notes)

- **참고 파일 처리**: `CLAUDE_andrej-karpathy.md`와 `기본 프롬프트.md`는 본 plan 범위 밖. 통합 완료 후 사용자가 다음 중 결정:
  - (A) 그대로 유지 (참고용 원본 보존)
  - (B) `docs/archive/`로 이동
  - (C) 삭제
- **사전 분석 결과**: 적용 가능 7항목 / 보류 7항목으로 분류 완료. 보류 사유는 사용자에게 사전 공유함.
- **목차 사전 승인**: 신규 섹션 배치 위치(상단 3개 + 중간 1개 + "개발 원칙" 5번 보강)에 대해 사용자 승인 완료.

### 진행 로그 (KST)

- 2026-05-03 10:17: plan 작성 시작 (Draft)
- 2026-05-03 10:17: 사용자 승인 — 상태 In Progress 전환
- 2026-05-03 10:20: Phase 1 완료 — 신규 섹션 4개 추가 + "개발 원칙" 5번 보강
- 2026-05-03 10:25: 마지막 Phase — black (146 files unchanged) + validate_project.py (passed=1025/failed=0/skipped=0) 통과 → ✅ Done

---
