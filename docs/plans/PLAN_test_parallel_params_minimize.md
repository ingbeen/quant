# Implementation Plan: 테스트 병렬처리 파라미터 최소화

> 작성/운영 규칙(SoT): 반드시 [docs/CLAUDE.md](../CLAUDE.md)를 참고하세요.
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 [docs/CLAUDE.md](../CLAUDE.md)를 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: [docs/CLAUDE.md](../CLAUDE.md) 섹션 3, 5 참고

---

**작성일**: 2026-01-28 20:30
**마지막 업데이트**: 2026-01-28 20:45
**관련 범위**: tests, utils
**관련 문서**: tests/CLAUDE.md, src/qbt/utils/CLAUDE.md

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 [docs/CLAUDE.md](../CLAUDE.md)를 따릅니다.

- Validation에서 `poetry run python validate_project.py`가 실패하면 **해당 Phase에서 즉시 수정 후 재검증**한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: 테스트 코드의 병렬처리 파라미터를 최소화하여 테스트 실행 시간 단축
- [x] 목표 2: 테스트 파라미터 최소화 규칙을 문서(tests/CLAUDE.md)에 추가

## 2) 비목표(Non-Goals)

- 프로덕션 병렬처리 로직 수정 (테스트 코드만 대상)
- 테스트 커버리지 감소 (검증 목적은 유지)
- 새로운 테스트 추가

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- 현재 `test_order_preservation` 테스트가 20개 입력을 사용하고 있어 불필요하게 실행 시간이 길다
- 병렬처리 테스트의 목적(순서 보장, 캐시 동작 등)을 검증하는데 최소한의 파라미터로 충분
- 테스트 파라미터 최소화 원칙이 문서에 명시되어 있지 않음

### 현재 파라미터 분석

| 테스트 | 현재 파라미터 | 최소화 가능 |
|-------|-------------|-----------|
| `test_basic_execution` | 5개 입력, 2 workers | 유지 (이미 최소) |
| `test_empty_inputs` | 0개 입력 | 유지 (예외 테스트) |
| `test_order_preservation` | **20개 입력, 4 workers** | **5개 입력, 2 workers로 감소** |
| `test_cache_initialization` | 2개 입력, 2 workers | 유지 (이미 최소) |
| `test_cache_clear_on_reinit` | 1개 입력씩 | 유지 (이미 최소) |
| `test_cache_read_only` | 1개 입력 | 유지 (이미 최소) |

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `CLAUDE.md` (루트)
- `tests/CLAUDE.md`
- `src/qbt/utils/CLAUDE.md`

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 docs/CLAUDE.md)

- [x] `test_order_preservation` 파라미터 최소화 (20개 → 5개 입력, 4 → 2 workers)
- [x] 테스트가 여전히 순서 보장 검증 목적을 달성
- [x] `poetry run python validate_project.py` 통과 (failed=0, skipped=0)
- [x] `poetry run black .` 실행 완료 (마지막 Phase에서 자동 포맷 적용)
- [x] `tests/CLAUDE.md`에 병렬처리 테스트 파라미터 최소화 규칙 추가
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- `tests/test_parallel_executor.py` - 테스트 파라미터 수정
- `tests/CLAUDE.md` - 병렬처리 테스트 파라미터 규칙 추가

### 데이터/결과 영향

- 테스트 실행 시간 감소
- 테스트 결과/동작에는 변화 없음 (동일한 검증 목적)

## 6) 단계별 계획(Phases)

### Phase 1 — 테스트 파라미터 최소화

**작업 내용**:

- [x] `test_order_preservation` 파라미터 수정:
  - 입력: `list(range(20))` → `list(range(5))`
  - workers: `max_workers=4` → `max_workers=2`
- [x] docstring 업데이트 (변경 사항 반영)

**Validation**:

- [x] `poetry run python validate_project.py` (passed=246, failed=0, skipped=0)

---

### Phase 2 (마지막) — 문서 업데이트 및 최종 검증

**작업 내용**

- [x] `tests/CLAUDE.md`에 병렬처리 테스트 파라미터 규칙 추가
  - 테스트 목적 달성에 필요한 최소한의 입력 사용
  - max_workers는 2를 기본으로 권장
  - 순서 보장 테스트도 최소 입력으로 충분
- [x] `poetry run black .` 실행(자동 포맷 적용)
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정

**Validation**:

- [x] `poetry run python validate_project.py` (passed=246, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

1. 테스트 / 병렬처리 테스트 파라미터 최소화로 실행 시간 단축
2. 테스트 / parallel_executor 테스트 효율화 및 규칙 문서화
3. 테스트 / 불필요한 테스트 파라미터 감소 + 문서 업데이트
4. 테스트 / test_order_preservation 입력 20→5개 최적화
5. 테스트 / 병렬처리 테스트 규칙 문서화 및 파라미터 정리

## 7) 리스크(Risks)

- 리스크 낮음: 단순 파라미터 감소로 검증 목적은 그대로 유지
- 파라미터를 너무 줄이면 엣지 케이스 누락 가능 → 5개 입력은 순서 보장 검증에 충분

## 8) 메모(Notes)

- 병렬처리 테스트의 핵심 목적:
  - `test_basic_execution`: 기본 동작 검증
  - `test_order_preservation`: **입력 순서 보장** (핵심 계약)
  - `test_cache_*`: 워커 캐시 메커니즘 검증
- 5개 입력 + 2 workers로도 순서 보장 검증은 충분 (as_completed 순서와 무관하게 결과 정렬됨)

### 진행 로그 (KST)

- 2026-01-28 20:30: 계획서 초안 작성
- 2026-01-28 20:35: Phase 1 완료 - 테스트 파라미터 수정, Validation 통과
- 2026-01-28 20:45: Phase 2 완료 - 문서 업데이트, 최종 Validation 통과, Done

---
