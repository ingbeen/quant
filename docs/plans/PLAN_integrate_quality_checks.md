# Implementation Plan: 품질 검증 스크립트 통합 (run_tests.sh → validate_project.py)

> 작성/운영 규칙(SoT): 반드시 [docs/CLAUDE.md](../CLAUDE.md)를 참고하세요.
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 [docs/CLAUDE.md](../CLAUDE.md)를 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: [docs/CLAUDE.md](../CLAUDE.md) 섹션 3, 5 참고

---

**작성일**: 2026-01-02 10:03
**마지막 업데이트**: 2026-01-02 10:23
**관련 범위**: scripts, tests, docs, utils
**관련 문서**: CLAUDE.md (루트), scripts/CLAUDE.md, tests/CLAUDE.md, docs/CLAUDE.md

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 [docs/CLAUDE.md](../CLAUDE.md)를 따릅니다.

- Validation에서 `poetry run python validate_project.py` 실패하면 **해당 Phase에서 즉시 수정 후 재검증**한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] `run_tests.sh` 제거하고 `check_code.py`를 `validate_project.py`로 통합
- [x] 테스트 실행(pytest)을 포함한 통합 품질 검증 스크립트 제공
- [x] 모든 품질 검증은 `validate_project.py`에서만 수행하도록 표준화
- [x] 관련 문서 업데이트하여 새로운 스크립트 사용법 및 직접 명령어 실행 금지 명시

## 2) 비목표(Non-Goals)

- `run_tests.sh`의 모듈별 세부 옵션 포팅 (analysis, data, strategy, tqqq, fast 등)
  - 이런 세부 옵션이 필요하면 예외적으로 직접 pytest 명령 사용 가능
- 새로운 테스트 추가
- 테스트 실행 방식 또는 pytest 설정 변경
- CI/CD 파이프라인 수정 (문서에만 기록)

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- 현재 프로젝트는 코드 품질 체크와 테스트 실행이 분리되어 있음:
  - `check_code.py`: ruff + mypy 통합 실행
  - `run_tests.sh`: pytest 실행 (다양한 옵션 제공)
- 개발자가 두 스크립트를 각각 실행해야 하므로 번거로움
- 직접 명령어 실행 시 일관성 없는 검증 방식 사용 위험
- 통합하면 단일 진입점으로 프로젝트 전체 검증 가능
- `check_code.py`보다 `validate_project.py`가 테스트를 포함한다는 의미를 더 명확히 전달

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `CLAUDE.md` (루트)
- `scripts/CLAUDE.md`
- `tests/CLAUDE.md`
- `docs/CLAUDE.md`
- `src/qbt/utils/CLAUDE.md`

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 docs/CLAUDE.md)

- [x] `run_tests.sh` 파일 제거 완료
- [x] `check_code.py`를 `validate_project.py`로 이름 변경
- [x] `validate_project.py`에 pytest 실행 기능 추가 (Ruff → Mypy → Pytest 순서)
- [x] argparse로 옵션 추가:
  - [x] `--only-lint`: ruff만 실행
  - [x] `--only-mypy`: mypy만 실행
  - [x] `--only-tests`: pytest만 실행
  - [x] `--cov`: 커버리지 포함 테스트 실행
- [x] `tests/CLAUDE.md` 업데이트 (직접 명령어 실행 금지 명시)
- [x] `docs/CLAUDE.md` 업데이트 (직접 명령어 실행 금지 명시)
- [x] 기타 CLAUDE.md 파일들에서 run_tests.sh 및 직접 명령어 참조 확인 및 업데이트
- [x] `poetry run python validate_project.py` 통과 (ruff + mypy + pytest)
- [x] `poetry run black .` 실행 완료 (마지막 Phase에서 자동 포맷 적용)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- `run_tests.sh` (제거)
- `check_code.py` (이름 변경: `validate_project.py`, 내용 수정)
- `tests/CLAUDE.md` (직접 명령어 실행 금지 명시)
- `docs/CLAUDE.md` (직접 명령어 실행 금지 명시)
- 루트 `CLAUDE.md` (직접 명령어 실행 금지 명시)
- 기타 `CLAUDE.md` 파일들 (run_tests.sh 참조 확인 및 업데이트)

### 데이터/결과 영향

- 데이터/결과 파일 영향 없음
- 스크립트 동작은 기존과 동일 (통합만 수행)

## 6) 단계별 계획(Phases)

### Phase 1 — 스크립트 통합 (check_code.py → validate_project.py)

**작업 내용**:

- [x] `check_code.py`를 `validate_project.py`로 이름 변경 (git mv)
- [x] `validate_project.py`에 pytest 실행 기능 추가
  - [x] `run_pytest(with_coverage: bool = False)` 함수 추가
    - `with_coverage=False`: 일반 테스트 실행
    - `with_coverage=True`: 커버리지 포함 테스트 실행
  - [x] `main()` 함수에 pytest 실행 추가 (Ruff → Mypy → Pytest 순서)
  - [x] argparse로 옵션 추가:
    - `--only-lint`: ruff만 실행
    - `--only-mypy`: mypy만 실행
    - `--only-tests`: pytest만 실행
    - `--cov`: 커버리지 포함 테스트 실행 (pytest와 함께 사용)
  - [x] 옵션 조합 검증 (--only-* 옵션은 상호 배타적)
  - [x] 최종 결과 요약 업데이트 (Ruff + Mypy + Pytest 통합)
- [x] Docstring 업데이트 (통합 품질 검증 스크립트로 변경)
- [x] 헬프 메시지 추가 (사용 예시 포함)
- [x] `run_tests.sh` 파일 제거 (git rm)

**Validation**:

- [x] `poetry run python validate_project.py` (전체 실행) - passed=182, failed=0, skipped=0
- [x] `poetry run python validate_project.py --only-lint` (ruff만) - 통과
- [x] `poetry run python validate_project.py --only-mypy` (mypy만) - 통과
- [x] `poetry run python validate_project.py --only-tests` (pytest만) - passed=182, failed=0, skipped=0
- [x] `poetry run python validate_project.py --cov` (전체 + 커버리지) - 통과

---

### Phase 2 — 문서 업데이트

**작업 내용**:

- [x] 루트 `CLAUDE.md` 업데이트
  - [x] "코딩 표준" 섹션에 품질 검증 규칙 추가
  - [x] **직접 명령어 실행 금지**: `poetry run ruff`, `poetry run mypy`, `poetry run pytest` 등 직접 실행 금지
  - [x] **표준 진입점**: `validate_project.py`만 사용
  - [x] **예외**: 특정 모듈/파일만 테스트할 때는 직접 pytest 명령 허용 (예: `poetry run pytest tests/test_strategy.py`)
- [x] `tests/CLAUDE.md` 업데이트
  - [x] "테스트 실행 방법" 섹션 전면 수정
  - [x] `./run_tests.sh` 참조를 `poetry run python validate_project.py`로 변경
  - [x] 품질 게이트 커맨드 섹션 업데이트
  - [x] 직접 명령어 실행 금지 명시
- [x] `docs/CLAUDE.md` 업데이트
  - [x] "포맷/린트/테스트 규칙" 섹션 전면 수정
  - [x] `./run_tests.sh` 참조를 `poetry run python validate_project.py`로 변경
  - [x] Black/코드 품질/테스트 실행 원칙 업데이트
  - [x] 직접 명령어 실행 금지 명시
- [x] 기타 `CLAUDE.md` 파일들 확인 및 업데이트
  - [x] `scripts/CLAUDE.md` 확인 (참조 없음)
  - [x] `src/qbt/utils/CLAUDE.md` 확인 (참조 없음)
  - [x] 기타 도메인 `CLAUDE.md` 확인 (backtest, tqqq 등) (참조 없음)
  - [x] `README.md` 업데이트 (빠른 시작, 주요 명령어, 문제 해결 섹션)
  - [x] `docs/plans/_template.md` 업데이트

**Validation**:

- [x] `poetry run python validate_project.py` - passed=182, failed=0, skipped=0

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

- [x] 모든 문서에서 run_tests.sh 및 직접 명령어 참조 제거 확인
  - [x] `grep -r "run_tests.sh" .` 실행하여 참조 확인 - 모두 제거됨 (archive 제외)
  - [x] `grep -r "poetry run ruff\|poetry run mypy\|poetry run pytest" docs/ tests/` 실행하여 직접 명령어 참조 확인 - 허용된 예외만 남음
- [x] `poetry run black .` 실행(자동 포맷 적용) - 이미 포맷 적용됨
- [x] 변경 기능 및 전체 플로우 최종 검증
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정

**Validation**:

- [x] `poetry run python validate_project.py` (passed=182, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

1. 개발도구 / 품질 검증 스크립트 통합 (validate_project.py)
2. 개발도구 / run_tests.sh 제거 및 validate_project.py로 통합
3. 리팩토링 / 품질 검증 단일 진입점 제공 (validate_project.py)
4. 개발도구 / 코드 품질+테스트 통합 실행 (validate_project.py)
5. 개발도구 / 품질 검증 표준화 (validate_project.py 단일 진입점)

## 7) 리스크(Risks)

- **외부 의존성**: `run_tests.sh`를 사용하는 외부 스크립트나 CI/CD 파이프라인이 있을 수 있음
  - 완화책: 문서에 변경 사항 명확히 기록, CI/CD 확인 필요
- **문서 업데이트 누락**: 여러 CLAUDE.md 파일에 run_tests.sh 및 직접 명령어 참조가 있을 수 있음
  - 완화책: grep으로 전체 검색 후 체계적으로 업데이트
- **pytest 옵션 누락**: run_tests.sh의 일부 유용한 옵션이 누락될 수 있음
  - 완화책: 필요 시 직접 pytest 명령 사용 (예외적으로 허용, 문서에 안내)

## 8) 메모(Notes)

### 주요 결정 사항

- **스크립트 이름**: `validate_project.py` (테스트 포함을 명시)
- **실행 순서**: Ruff → Mypy → Pytest (빠른 체크부터 실행하여 조기 실패)
- **옵션 설계**:
  - 기본 (옵션 없음): 전체 실행 (Ruff + Mypy + Pytest)
  - `--only-lint`: ruff만 실행
  - `--only-mypy`: mypy만 실행
  - `--only-tests`: pytest만 실행
  - `--cov`: 커버리지 포함 (전체 실행 또는 --only-tests와 함께 사용)
- **직접 명령어 실행 금지**:
  - 원칙: `validate_project.py`만 사용
  - 예외: 특정 모듈/파일만 테스트할 때 (예: `poetry run pytest tests/test_strategy.py`)
- **run_tests.sh 포팅 범위**:
  - 포팅: 전체 테스트 실행, 커버리지 옵션
  - 미포팅: 모듈별 세부 옵션 (analysis, data, strategy, tqqq, fast 등)

### 진행 로그 (KST)

- 2026-01-02 10:03: 계획서 작성 시작
- 2026-01-02 10:06: 사용자 피드백 반영 (스킵 옵션 제거, cov 포함, 직접 명령어 금지 명시)
- 2026-01-02 10:14: 계획서 승인, Phase 1 시작
- 2026-01-02 10:17: Phase 1 완료, Phase 2 시작
- 2026-01-02 10:21: Phase 2 완료, 마지막 Phase 시작
- 2026-01-02 10:23: 모든 Phase 완료, 최종 검증 통과, Done 처리

---
